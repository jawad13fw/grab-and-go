# TODO

- [x] Update `frontend/src/pages/Admin/Dashboard.jsx` to remove call to missing `adminApi.getStats()`.
- [x] Replace with `adminApi.getAnalytics()` and map response fields to the dashboard cards.

- [x] Ensure `recent activity` section renders safely (no undefined field crashes).
- [ ] Run a frontend build/test command (as available) to ensure no syntax/runtime import issues (blocked due to PowerShell execution policy).



